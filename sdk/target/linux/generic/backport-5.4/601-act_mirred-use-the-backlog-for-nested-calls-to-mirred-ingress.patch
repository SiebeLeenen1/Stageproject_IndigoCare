--- a/net/sched/act_mirred.c
+++ b/net/sched/act_mirred.c
@@ -305,7 +305,7 @@ static int tcf_mirred_act(struct sk_buff
 	if (!want_ingress)
 		err = dev_queue_xmit(skb2);
 	else
-		err = netif_receive_skb(skb2);
+	        err = netif_rx(skb);
 
 	if (err) {
 out:
